fn fast_mul(x: u16, y: u16) -> u16 {
	if y & 1 == 0 {
		let z: u16 = mul(x, y >> 1);
		return z + z;
	}
	else {
		return x + mul(x, y - 1);
	}
}

fn dumb_mul(x: u16, y: u16) -> u16 {
	let ans: u16 = 0;
	let i: u16 = y;
	while i < y {
		ans = ans + x;
		i = i + 1;
	}
	return ans;
}
